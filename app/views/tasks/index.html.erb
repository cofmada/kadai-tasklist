<h1>タスクの一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>No.<%= link_to task.id, task %> : <%= task.content %> &rArr;<%= task.status %></li>
  <% end %>
</ul>

<%== pagy_nav(@pagy) %>

<%= link_to '新規タスクの登録', new_task_path %>
